// earlier classes, modules were not there.. so there is difference between old and new JS, but we can still use old codes
//but just to use new JS "use strict"

"use strict"; // treat all JS code as newer version.. now a days no need to defione use strct it's automatically considered as newer

// alert("hello")    // we are using node.js not browser

let name = "Renuka"    // string
let age = 25           //number
let isLoggedin = false // boolean


/*
number    => 2 to power 53
bigint
string    => "" or ''
boolean   => true or false
null      => standalone value = empty value =>typeof(null)=>object
undefined => value is not defined 
symbol    =>unique --> to find uniqueness-- most used in react

object

*/

console.log(typeof null)